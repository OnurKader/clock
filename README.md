# C Program to Show Time in the Terminal

## Installation
Clone the repository and run `make` in the `src/` directory, this will build into `bin/`

To install run `sudo make install` to link the binary to `/usr/bin/`
